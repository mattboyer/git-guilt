language: python

python:
        - "2.7"
        - "3.2"

install:
        - "pip install -r requirements.txt"

script:
        - pep8 src
        - nosetests test/ --with-coverage --cover-package=guilt
        - pylint src/*

after_success:
        coveralls
